type Query {
  version: String!
  company(id: ID!): Company
  companies(country: String): [Company!]!
  employee(id: ID!): Employee
  employees: [Employee!]!
  team(id: ID!): Team
  teams: [Team!]!
}

type Company {
  id: ID!
  name: String!
  slogan: String!
  phone: String!
  founded: String!
  country: String!
  email: String!
  website: String!
}

type Employee {
  id: ID!
  lastName: String!
  firstName: String!
  title: String!
  phone: String!
  profile: String!
  avatar: String!
  salary: String!
}

type Team {
  id: ID!
  name: String!
}
